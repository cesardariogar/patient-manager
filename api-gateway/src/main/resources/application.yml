---
# =============== API-Gateway ==================== #
server:
  port: 4004  # Gateway listens on port 4004

spring:
#  profiles:
#    active: local
  output:
    ansi:
      enabled: always
  application:
    name: api-gateway